export enum ItemType {
    Document = 'document',
    Secret = 'secret',
    Equipment = 'equipment',
}

export enum TransactionType {
    Supply = 'supply',
    Return = 'return',
}

export enum Role {
    Admin = 'admin',
    User = 'user',
}
